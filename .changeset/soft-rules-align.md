---
"@narumitw/pi-tui-kit": minor
---

Add a width-safe `HorizontalRule` component with inset, labeled, aligned, sanitized, and callback-styled rendering.
Use themed top and bottom rules consistently across every standard TUI screen.
